let menu = document.getElementById("menu");
let menuList = document.getElementById("menuList");

menu.addEventListener("click", () => {
    menuList.classList === menuList.classList. ? alert('yes') : alert('no') 
    // menuList.classList === "hidden" ? menuList.classList.remove("hidden") : menuList.classList.add("hidden") 
});
